import { DateTillTodayPipe } from './date-till-today.pipe';

describe('DateTillTodayPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTillTodayPipe();
    expect(pipe).toBeTruthy();
  });
});
