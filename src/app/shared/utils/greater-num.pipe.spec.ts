import { GreaterNumPipe } from './greater-num.pipe';

describe('GreaterNumPipe', () => {
  it('create an instance', () => {
    const pipe = new GreaterNumPipe();
    expect(pipe).toBeTruthy();
  });
});
