import { FieldTooltipPipe } from './field-tooltip.pipe';

describe('FieldTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
