import { NameDestructPipe } from './name-destruct.pipe';

describe('NameDestructPipe', () => {
  it('create an instance', () => {
    const pipe = new NameDestructPipe();
    expect(pipe).toBeTruthy();
  });
});
